// Re-export from the centralised store so existing imports keep working.
export type { CompressResult } from "./useCompressStore";
export { useCompressStore as useImageCompress } from "./useCompressStore";
